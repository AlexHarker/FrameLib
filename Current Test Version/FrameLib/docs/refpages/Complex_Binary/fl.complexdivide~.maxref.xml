<c74object category="Complex_Binary" module="FrameLib" name="fl.complexdivide~"> 
 
    <digest> 
        Divides the left complex pair of input frame values by the right complex pair of input frame values.
    </digest> 
 
    <description> 
         Calculation is performed on pairs of complex values in turn (with each complex value split across two inputs). If there is a mismatch between frame lengths within each complex pair (within a single operand) the shorter input is padded with zeros before calculation and the longer size used). The outputs are frames at least as long as the smaller of the two input pairs. When complex pairs of frames (left and right operands) mismatch in size the result depends on the setting of the mismatch parameter. Either or both pairs of inputs may be set to trigger output.
    </description> 
 
    <misc name="Parameters"> 
 
        <entry name="0. /mismatch [enum]"> 
            <description> 
                Sets the mode used when dealing with mismatched input lengths: wrap - the smaller input is read modulo against the larger input. shrink - the output length is set to the size of the smaller input. pad_in - the smaller input is padded prior to calculation to match the size of the larger input. pad_out - the output is padded to match the size of the larger input.<br /> 
                <bullet>[0] - wrap</bullet> 
                <bullet>[1] - shrink</bullet> 
                <bullet>[2] - pad_in</bullet> 
                <bullet>[3] - pad_out</bullet>
            </description> 
        </entry> 
 
        <entry name="1. /trigger_ins [enum]"> 
            <description> 
                Sets which inputs trigger output.<br /> 
                <bullet>[0] - both</bullet> 
                <bullet>[1] - left</bullet> 
                <bullet>[2] - right</bullet>
            </description> 
        </entry> 
 
        <entry name="2. /pad [fixed length double array]"> 
            <description> 
                Sets the complex value used for padding (for either pad_in or pad_out modes).
            </description> 
        </entry> 
 
    </misc> 
 
    <metadatalist> 
        <metadata name="author">Alex Harker</metadata> 
        <metadata name="tag">FrameLib</metadata> 
        <metadata name="tag">Complex_Binary</metadata> 
    </metadatalist> 
 
    <seealsolist> 
    </seealsolist> 
    <misc name="Discussion"> 
        <entry name="Keywords"> 
            <description> 
                boilerplate keywords
            </description> 
        </entry> 
    </misc> 
 
</c74object>